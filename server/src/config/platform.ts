/** Paystack merchant currency ISO code — GHS for Ghana accounts. */
export const PLATFORM_CURRENCY = process.env.PLATFORM_CURRENCY ?? "GHS";

/** Currency symbol for UI display purposes only */
export const PLATFORM_CURRENCY_SYMBOL = "GH₵";